<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lotta's Node Express Blog | <%= title %></title>
</head>
<body>
    <%- include('./partials/nav.ejs')%>
<div>
    <h2>Developers Discovery</h2>
    <% if(blogs.length > 0) { %>
       <% blogs.forEach(blog => { %>
        <h3> <%= blog.title %></h3>
        <p><%= blog.snippet %></p>

        <% }) %>
   <% } else {%>
    <p>No Blogs to display</p>
    <% } %>

    

</div>
</body>
</html>